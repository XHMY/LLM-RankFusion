Adapt from [RankAggregation-Lib](https://github.com/nercms-mmap/RankAggregation-Lib)

Only include Python Implementation of unsupervised aggregation methods from the original repo.

Minor change to the original detail but the overall function is the same.